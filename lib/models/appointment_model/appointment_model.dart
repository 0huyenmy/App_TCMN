class AppointmentModel {
  final String? image;
  final String? title;
  final String? address;
  final String? rate;
  final String? distance;

  AppointmentModel({
     this.image,
     this.title,
     this.address,
     this.rate,
     this.distance,
  });
}